ollama>=0.3.0
ddgs>=6.0.0
requests>=2.31.0
beautifulsoup4>=4.12.0
mcp>=1.2.0
markdown>=3.5
xhtml2pdf>=0.2.11
flask>=3.0

# Optional — only needed for the alternative LangChain version
# (langchain_agent.py). The core agent does not use these.
langchain-ollama>=0.2
langgraph>=0.2
